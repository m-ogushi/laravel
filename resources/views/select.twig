ログインしたトップページ

<form action="select" method="post">
<p><br>
<input type="radio" name="already" value="1" checked> まだの人のみ
<input type="radio" name="already" value="0"> 全員
</p>
<input class="btn-login" type="submit" value="抽選開始"/></a>
<input type="hidden" name="_token" value="{{ csrf_token() }}">
</form>
<ul>
{% set name = [] %}
{% for item in users %}
{% set name = item %}
{{ name.name }}
{% endfor %}
{{ session('status') }}
</ul>
{{ name.id }}
{{ session('status') }}
<form action="update" method="post">
<input class="btn-login" type="submit" value="スピーチする"/></a>
<input type="hidden" name="memberId" value="{{ name.id }}">
<input type="hidden" name="membername" value="{{ name.name }}">
<input type="hidden" name="_token" value="{{ csrf_token() }}">
</form>
